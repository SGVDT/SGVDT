'use strict'

const express = require('express');
const User = require(__dirname + '/../model/user');
const jsonParser = require('body-parser').json();
const basicHTTP = requre(__dirname + '/../LIB/');
